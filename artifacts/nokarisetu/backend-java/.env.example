# Copy to your local environment manager. Never commit real credentials.
#
# Database
DATABASE_URL=jdbc:postgresql://localhost:5432/nokarisetu
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=replace-me

# SMTP for automatic application and moderation emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=replace-me
SMTP_PASSWORD=replace-me

# Optional third-party API keys belong here, not in React source files.
# JOBS_API_KEY=
# ANALYTICS_API_KEY=